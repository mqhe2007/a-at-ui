# 2026-08-13

## 项目改名：a@ui / a-at-ui → braid

- 背景：原名字 `a@ui` 的 `@` 符号造成传播障碍（URL 编码、命令行、复制粘贴歧义），与用户多轮讨论后定案新品牌名 **braid**（单词语义、具象、无宗教联想；npm 实测空闲）。
- 全量执行内容：
  - 本地根目录 `/Users/mengqinghe/代码/a@ui` → `/Users/mengqinghe/代码/braid`
  - npm 包名 `a-at-ui` → `braid`（packages/braid/package.json，name=braid，repository/hompage 同步更新）
  - 内部目录：packages/a-at-ui → packages/braid；skills/a-at-ui-{manifest,setup,protocol} → skills/braid-{manifest,setup,protocol}
  - 代码标识符：`AAtUI` → `Braid`（如 createAAtUIPlugin → createBraidPlugin、validateAAtUICommand → validateBraidCommand、AAtUICommand → BraidCommand），属 breaking change，项目处于 0.x pre-stable 可接受
  - 品牌名 `A@UI` → `Braid`（README、文档站内容、og-image.svg、CSS 等）
  - 全部链接：GitHub mqhe2007/a-at-ui → mqhe2007/braid；文档站 a-at-ui.mengqinghe.com → braid.mengqinghe.com
- 验证：bun install 重建 lockfile；build 成功；12 个测试全部通过；构建产物无旧名残留。
- 待用户完成（平台侧）：
  1. GitHub 仓库改名 mqhe2007/a-at-ui → mqhe2007/braid，并更新本地 remote
  2. npm publish 发布 braid 包；npm deprecate a-at-ui "renamed to braid"
  3. DNS/域名 a-at-ui.mengqinghe.com → braid.mengqinghe.com（或 301）
  4. git commit + push 改名变更

## 用户命名偏好（长期）

- 技术/产品命名：要求**单词语义**（不要连字符组合如 a-ui、trio-ui）；**具象不抽象**（否掉 triune："过于抽象且带宗教色彩"）；无宗教联想；npm 包名需实测空闲（三批候选词全部实测过）。
- 决策方式：喜欢先给多方案 + 数据证据（npm 占用实测），再收敛定案。
